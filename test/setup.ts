import { beforeAll, afterAll, beforeEach, afterEach } from "vitest";

process.env.JWT_SECRET = "test-jwt-secret";

beforeAll(async () => {

});

afterAll(async () => {

});

beforeEach(async () => {

});

afterEach(async () => {

});